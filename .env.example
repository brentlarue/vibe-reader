# Application Password
# Set a strong password for authentication
APP_PASSWORD=your-strong-password-here

# Session Secret
# Used to sign session cookies. Generate a random string (e.g., using: openssl rand -hex 32)
# If not set, a random secret will be generated on each server restart (sessions will not persist)
SESSION_SECRET=your-random-session-secret-here

# OpenAI API Key (for AI features)
OPENAI_API_KEY=your-openai-api-key-here

# Server Port (optional, defaults to 3001)
# PORT=3001
